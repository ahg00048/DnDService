spring.mongodb.database: "dndservice_db_board"

# Eureka config
# eureka.instance.instance-id: "${spring.application.name}:${random.uuid}"
eureka.client.enabled: false

spring.rabbitmq.virtual-host: "vLobbiesStomp"